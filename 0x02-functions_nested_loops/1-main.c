#include "main.h"

/**
 * main - check the code
 * print_alphabet - prints the alphabe, in lowercase, followed by a new line
 * Return: Always 0.
 */
int main(void)
{
    print_alphabet();
    return (0);
}
